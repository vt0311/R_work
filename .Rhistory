help()
install.r()
install.R()
# 1.색상의 유형별로 빈도 수와 비율에 대한 기술 통계량을 구하시오.
setwd('C:/R_work')
install.packages('Hmisc')
library(Hmisc)
# 데이터 테이블의 빈도분석 중심의 기술통계량 분석을 한번에 해주는 기능함수를 가진 패키지
install.packages('prettyR')
library(prettyR)
table(mycar$color)
mycar <- read.csv('mycar.csv', header=TRUE)
table(mycar$color)
table(mycar[2])
table(mycar[2])
prop.table(table(mycar$color))
prop.table(table(mycar[2]))
prop.table(table(mycar[2]))*100
# 소수점 첫째자리까지 반올림
round(prop.table(table(mycar[2]))*100)
# 소수점 첫째자리까지 반올림
round(prop.table(table(mycar[2]))*100, 1)
surveyFreq <- c(table(mycor$color))
surveyFreq <- c(table(mycar$color))
surveyProp <- round(prop.table(table(mycar$color))*100, 1)
surveytable <- data.frame(Freq=surveyFreq, Prop=surveyProp)
surveytable
surveyProp <- c(round(prop.table(table(mycar$color))*100, 1))
surveytable <- data.frame(Freq=surveyFreq, Prop=surveyProp)
surveytable
describe(mycar)
describe(mycar$color)
freq(mycar)
freq(mycar$color)
chisq.test(surveyFreq)
freq(mycar$color)
freq(mycar)
# 3.패키지를 이용하여 색상 유형에 따른 빈도와 비율 값을 구해보시오.
describe(mycar)
# 3.패키지를 이용하여 색상 유형에 따른 빈도와 비율 값을 구해보시오.
#describe(mycar)
describe(mycar$color)
surveytable
describe(mycar)
describe(mycar$color)
mytooth <- read.csv('mytooth.csv', header=TRUE)
table(mytooth$buy)
prop.table(table(mytooth$buy))
prop_tb_mybuy <- prop.table(table(mytooth$buy))
round(prop_tb_mybuy*100, 2)
# 백분율 표현
round(prop_tb_mybuy*100, 2)
buyFreq <- c(tb_mybuy)
# 2.구매 여부의 빈도 수와 비율을 테이블 형태로 표현하시오.
tb_mybuy <- table(mytooth$buy)
buyFreq <- c(tb_mybuy)
buyProp <- c(round(prop.table(table(mytooth$buy))*100, 1))
buytable <- date.frame(Freq = buyFreq, Prop = buyProp )
buytable
buytable <- data.frame(Freq = buyFreq, Prop = buyProp )
buytable
print(buytable)
view(buytable)
view(buytable)
buytable
buyProp <- c(round(prop.table(tb_mybuy)*100, 1))
buytable <- data.frame(Freq = buyFreq, Prop = buyProp )
buytable
# 3.패키지를 이용하여 구매 여부에 따른 빈도와 비율 값을 구해 보시오.
describe(mytooth$buy)
mytooth <- read.csv('C:\\Users\\RYZEN-HSW\\Desktop\\N06AB_2012_RE2.csv', header=TRUE)
head(mytooth)
mytooth <- read.csv('mytooth.csv', header=TRUE)
head(mytooth)

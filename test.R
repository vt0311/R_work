switch('name')

mymax <- function(a, b) {
  imsi <- ifesle(a -b>0, a, b)
  return (imsi) 
}

mymax(5, 4)

start = 1
end = 10
total = 0

i <- c(start:end)

for (k in i) {
  total <- total + k
}
  print(total)
  
  help(cat)

scan(what = '' )
